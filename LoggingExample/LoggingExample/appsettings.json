{
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    },
    "GELF": {
      "Host": "YOUR IP HERE",
      "Port": 12202, // Not required if using default 12201.
      "Protocol": "UDP", // Not required if using default UDP.
      "LogSource": "Shlomy", // Not required if set in code as above.
      "AdditionalFields": { // Optional fields added to all logs.
        "AdditionalField": "Yes"
      },
      "LogLevel": {
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    }
  }
}